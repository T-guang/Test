# V2.3.9.1 Demo 场景运行态取证

## 目的

`Demo.unity` 静态审计发现根层存在大量 `CircuitComponent`、`TerminalView` 与 `WireView`。当前源码证据显示 `WorkspaceController` 主要分析其私有 `components` 列表与 `WireManager.Wires`，但这不足以证明根层历史对象在 Play Mode 中一定不会污染 Analyzer 或 Validation。

本轮不删除、移动或保存任何场景对象。结论只能来自真实 Play Mode 计数。

## 取证入口

进入 Play Mode，等待默认示例加载后打开：

`Tools/Diagnostics/采集 Demo 场景运行态对象计数`

每完成一个阶段，在窗口填写阶段名并点击“采集当前阶段”；所有阶段完成后点击“导出取证 JSON”。工具仅读取当前场景，并将证据写入：

`Assets/EditorTests/Baselines/V2.3.9.1/DemoSceneRuntimeEvidence.json`

建议阶段名固定为：

1. `初始默认示例`；
2. `清空画布`；
3. `加载家庭模板`；
4. `加载工业模板`；
5. `保存并导入用户图纸`。

## 每阶段字段

- 全场景 `CircuitComponent`、`TerminalView`、`WireView` 数；
- 场景根对象数；
- 根层电路对象数；
- `workspace.Components.Count`；
- `workspace.WireManager.Wires.Count`；
- 私有 ComponentLayer / WireLayer 子对象数（只读反射）；
- 本次 Analyzer 与 Validation 实际传入的组件/导线数；
- Analyzer 是否返回；
- Validation issue 数。

Analyzer 与 Validation 的输入数来自同一次实际调用：

```csharp
analyzer.Analyze(workspace.Components, workspace.WireManager.Wires);
validation.Validate(workspace.Components, workspace.WireManager.Wires, state);
```

因此可直接回答根层残留是否进入当前工作区输入，而不是仅统计全场景对象总数。

## 页面生命周期轻量回归

依次切换六个顶部页面 20 轮：模拟电路、图纸集、仿真广场、元器件百科、常用工具、系统信息。完成后在同一窗口填写“切换六个页面 20 轮后”，点击“采集页面生命周期计数”，再导出。

记录：

- `LocalInspectorPanel`；
- `SimulationGalleryPageController`；
- `LocalProfilePageController`；
- `CommonToolsPageController`；
- `TemplateLoadController`；
- `EventSystem`；
- `Canvas`；
- 页面根对象数量。

本轮不通过反射读取或修改 Button listener，只用 Controller/Canvas/EventSystem 对象数发现重复创建风险。

## 判定规则

| 观察 | 结论 |
|---|---|
| 全场景对象很多，但 Workspace/Analyzer/Validation 输入仅随当前默认示例或模板变化 | 根层对象暂不构成已证实的运行态污染；单独立项处理场景源头，不要立即删除。 |
| 清空画布后 Workspace 与 Layer 都归零，而全场景根层对象仍存在 | 历史对象未由 `ClearDrawing` 管理，应继续检查其用途与启动成本。 |
| 模板加载后 Workspace 输入与全场景对象叠加增长，或 Validation 输入包含残留 | P0/P1 场景双重来源风险；停止重构，先定点处理场景装配。 |
| 20 轮页面切换后任一 Controller、Canvas 或 EventSystem 递增 | P1 重复创建风险；优先修复页面幂等性。 |
| 计数稳定、无 Console Error | 可以把“根层对象是否污染运行态”从假设降为已验证无污染，但仍保留场景清理候选项。 |

## 已采集结果

采集时间：2026-07-11。证据文件：[DemoSceneRuntimeEvidence.json](../Assets/EditorTests/Baselines/V2.3.9.1/DemoSceneRuntimeEvidence.json)，`schemaVersion = 3`。窗口在打开期间监听 Unity `Error`、`Exception` 与 `Assert` 日志，并在每次阶段采集后重置该阶段计数。

| 阶段 | Scene CircuitComponent | Scene TerminalView | Scene WireView | Workspace Components | Workspace Wires | Analyzer 输入 | Validation 输入 | Validation Issue | Console Error |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 初始默认示例 | 422 | 1805 | 774 | 6 | 9 | 6 / 9 | 6 / 9 | 0 | 0 |
| 清空画布 | 416 | 1787 | 765 | 0 | 0 | 0 / 0 | 0 / 0 | 0 | 0 |
| 加载家庭模板 | 420 | 1799 | 770 | 4 | 5 | 4 / 5 | 4 / 5 | 0 | 0 |
| 加载工业模板 | 423 | 1824 | 782 | 7 | 17 | 7 / 17 | 7 / 17 | 0 | 0 |
| 保存并导入用户图纸 | 423 | 1824 | 782 | 7 | 17 | 7 / 17 | 7 / 17 | 0 | 0 |

### 结论

1. 根层/场景历史电路对象会保留在全场景计数中：清空画布后仍有 416 个 `CircuitComponent`、1787 个 `TerminalView`、765 个 `WireView`；
2. 它们不进入当前 `WorkspaceController.Components`、`WireManager.Wires`、Analyzer 或 Validation 输入：清空后这四组输入均为 0；
3. 家庭模板、工业模板与保存后重新导入均只反映当前工作区对象数；工业模板加载后再导入的 Scene/Workspace 计数完全一致，没有模板叠加证据；
4. 每阶段 `validationIssueCount` 均为 0，真实监听得到的 `consoleErrorsSinceCapture` 均为 0；
5. 清空画布阶段的 `CircuitRuleChecker.FilterCheckPanelFalsePositives` 报告 3 个业务检查 Error，属于空画布的旧检查器结果，不是 Console Error，也不是 Validation RuleId；
6. 结论是“当前没有已证实的运行态污染”，但静态场景仍保留大量历史对象。场景清理应单独立项，先确认编辑用途、启动成本与序列化来源；本轮不删除。

### 页面生命周期

完成六个顶部页面 20 轮切换后：

- `LocalInspectorPanel`：1；
- `SimulationGalleryPageController`：1；
- `LocalProfilePageController`：1；
- `CommonToolsPageController`：1；
- `TemplateLoadController`：1；
- `EventSystem`：1；
- `Canvas`：2；
- Console Error：未记录。

当前没有 Controller、EventSystem 或 Canvas 重复创建证据。
